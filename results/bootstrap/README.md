# Bootstrap reproduction outputs

These files record structural acceptance values from the 2026-09-23 bootstrap. Timings are intentionally omitted from the canonical summaries.

`SUMMARY.json` is descriptive evidence, not the project status authority. Re-run `./scripts/verify_baseline.sh` from a clean checkout to regenerate the full check under `.build/baseline/`.
